# CodeQL Security Analysis
#
# Scope: Analyzes GitHub Actions workflow files (YAML) only.
# Note: PowerShell scripts are NOT analyzed by CodeQL (unsupported language).
#       PowerShell security is handled by PSScriptAnalyzer (ci.yml) and
#       custom security checks (security.yml).

name: CodeQL

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '**.yml'
      - '**.yaml'
  pull_request:
    branches: [main]
    paths:
      - '**.yml'
      - '**.yaml'
  schedule:
    - cron: '0 6 * * 1'  # Weekly on Monday 6am UTC

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name != 'schedule' }}

permissions: {}

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    timeout-minutes: 15

    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@5ef0c079ce82195b2a36a210272d6b661572d83e # v2.14.2
        with:
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            github.com:443
            objects.githubusercontent.com:443
            release-assets.githubusercontent.com:443
            uploads.github.com:443

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      # Scope: This workflow only analyzes GitHub Actions workflows (YAML files).
      # CodeQL does not support PowerShell - PowerShell scripts are analyzed by:
      # - PSScriptAnalyzer in ci.yml (lint and security rules)
      # - Custom security checks in security.yml (hardcoded secrets, unsafe patterns)
      - name: Initialize CodeQL
        uses: github/codeql-action/init@b13d724d35ff0a814e21683638ed68ed34cf53d1 # v4
        with:
          languages: actions
          queries: security-and-quality

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@b13d724d35ff0a814e21683638ed68ed34cf53d1 # v4
        with:
          category: "/language:actions"

      - name: Summary
        if: always()
        run: |
          echo "## CodeQL Analysis Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Languages analyzed:** GitHub Actions" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Results are available in the [Security tab](https://github.com/${{ github.repository }}/security/code-scanning)." >> $GITHUB_STEP_SUMMARY
